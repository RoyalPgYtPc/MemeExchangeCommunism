# MemeExchangeCommunism

Static site lives in `docs/`.

## Firebase Hosting

Deploy from the repository root:

```powershell
firebase deploy
```

Preview locally:

```powershell
firebase emulators:start --only hosting
```
